<Window x:Class="WarehouseNickWPF.InvoicesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Накладные" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Создать накладную" Command="{Binding CreateInvoiceCommand}" Width="140" Margin="5"/>
            <Button Content="Изменить накладную" Command="{Binding EditInvoiceCommand}" Width="140" Margin="5"/>
            <Button Content="Удалить накладную" Command="{Binding DeleteInvoiceCommand}" Width="140" Margin="5"/>
            <Button Content="Подтвердить" Command="{Binding ConfirmInvoiceCommand}" Width="100" Margin="5"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Invoices}" SelectedItem="{Binding SelectedInvoice}" AutoGenerateColumns="True"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Закрыть" Click="CloseButton_Click" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>