<Window x:Class="WarehouseNickWPF.Dialogs.InvoiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Накладная" Height="500" Width="650"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Номер:" Width="50" VerticalAlignment="Center"/>
            <TextBox x:Name="NumberTextBox" Width="150" Margin="0,0,10,0"/>
            <TextBlock Text="Дата:" Width="40" VerticalAlignment="Center"/>
            <DatePicker x:Name="DatePicker" SelectedDate="{x:Static sys:DateTime.Now}" Width="120" Margin="0,0,10,0"/>
            <TextBlock Text="Тип:" Width="40" VerticalAlignment="Center"/>
            <ComboBox x:Name="TypeComboBox" Width="100">
                <ComboBoxItem Content="Приход" Tag="Incoming" IsSelected="True"/>
                <ComboBoxItem Content="Расход" Tag="Outgoing"/>
            </ComboBox>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="ItemsDataGrid" AutoGenerateColumns="False" CanUserAddRows="True" 
                  ItemsSource="{Binding Items, RelativeSource={RelativeSource AncestorType=Window}}">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Товар" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Product.Name}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}, Path=DataContext.AvailableProducts}"
                      DisplayMemberPath="Name"
                      SelectedValuePath="Id"
                      SelectedItem="{Binding Product}"
                      IsSynchronizedWithCurrentItem="False"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
                <DataGridTemplateColumn Header="Удалить" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="X" Click="RemoveItem_Click" Tag="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Click="OkButton_Click" Width="75" Margin="5"/>
            <Button Content="Отмена" Click="CancelButton_Click" Width="75" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>